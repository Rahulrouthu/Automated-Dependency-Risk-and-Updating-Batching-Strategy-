#!/bin/sh
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
if [ -f "$DIR/../.tools/apache-maven-3.9.9/bin/mvn" ]; then
    "$DIR/../.tools/apache-maven-3.9.9/bin/mvn" "$@"
else
    mvn "$@"
fi
